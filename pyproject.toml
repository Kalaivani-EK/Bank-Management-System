[project]
name = "bank-management-system"
version = "1.0.0"
requires-python = ">=3.12"

dependencies = [
    "Flask",
    "Flask-SQLAlchemy",
    "Flask-JWT-Extended",
    "Flask-Cors"
]

[tool.vercel]
entrypoint = "backend.app:app"
